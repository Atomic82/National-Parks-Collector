<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
  <h1>Parks! 🏞🏞🏞</h1>
  <h2>All the Parks!</h2>
  <ul>
    <% parks.forEach(park => { %>
        <li>🏞 <%= park.name %> is <%= park.visited ? "visited" : "haven't been yet!"%> </li>
    <% }) %>
  </ul>
</section>

<%- include('../partials/footer') %>